# Лабораторная работа №1 - М3
***Калькулятор в обратной польской нотации
(Reverse Polish Notation - RPN)***

Используемые библиотеки: pytest, math, re.

Считает выражения в обратной польской нотации.

Позволяет использовать скобки (в них должно быть корректное выражение RPN. Оно заменяется в исходной строке на значение).

Обрабатывает унарные +/- ($/~). Допускается 1 унарный знак перед числом или скобкой или его отсутствие.


**Поддерживает**:

    целые числа (5, ~6)
    числа с плавающей точкой (5.0(преобразуется в целое), 5.5, ~8.09)
    экспоненциальную форму записи числа (5e5 = 5 * 10**5)
	число Эйлера (E = 2.71...)
	число Пи (π = 3.14...)


**Поддерживает операции**:

	"+" - сложение. a b + = a + b
	"-" - вычитание. a b - = a - b
	"/" - деление. a b / = a / b
	"*" - умножение. a b * = a * b
	"**" - возведение в степень. a b ** = a ** b
	"//" - целочисленное деление (только для целых). a b // = a // b
	"%" - остаток от деления (только для целых). a b % = a % b.


**Установка:**

		1. git clone https://github.com/spTrent/Calculator.git
		2. cd Calculator


>**Для разработчика (в терминале IDE с установленным uv)**:
>
>	
>	>*(**Установка uv в терминале**):
>	>
>	>		sudo apt update
>	>
>	>		sudo apt install curl unzip -y
>	>
>	>		curl -LsSf https://astral.sh/uv/install.sh | sh
>	>
>	>		echo 'export PATH="$HOME/.local/bin:$PATH"' >> ~/.bashrc
>	>
>	>		source ~/.bashrc
>
>
>		 3. uv sync
>		 4. source .venv/bin/activate


**Запуск**:

	Для пользователя: python3 -m src.main
	Для разработчика: uv run -m src.main
